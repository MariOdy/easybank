import React from "react";
import styles from "./styles.module.scss";

const RequestInvite: React.FC = () => {
  return <button className={styles.request_btn}>Request Invite</button>;
};

export default RequestInvite;
